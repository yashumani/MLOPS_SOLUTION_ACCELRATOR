# Top-level pinned source for reproducible builds.
# Compile to requirements.lock with:
#   pip-compile --generate-hashes --output-file=requirements.lock requirements.in
#
# Versions reflect what is currently provisioned in the
# ``mlops-v3-unified:23`` Azure ML environment. Pin upper bounds to avoid
# surprise breakage; tighten exact versions before each release.

pandas>=2.0,<3.0
numpy>=1.24,<2.0
scikit-learn>=1.3,<1.6
pandera>=0.17,<0.20
imbalanced-learn>=0.11,<0.13
boruta>=0.3,<0.5
pycaret>=3.2,<3.4
flaml>=2.1,<2.4
mlflow>=2.8,<2.18
pyyaml>=6.0,<7.0
great-expectations>=0.18,<1.0
optuna>=3.4,<4.1

# Azure ML SDK V2
azure-ai-ml>=1.11.0,<2.0
azure-identity>=1.14.0,<2.0
azure-storage-blob>=12.17.0,<13.0
azureml-mlflow>=1.51.0,<2.0

# Drift monitoring
evidently>=0.4,<0.5

# Test & coverage tooling
pytest>=7.0,<9.0
pytest-cov>=4.0,<6.0
