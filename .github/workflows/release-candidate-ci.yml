name: Release Candidate CI

on:
  push:
    branches:
      - "codex_ys/**"
      - "feature/**"
      - "dev"
      - "dev-*"
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: release-candidate-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  backend:
    name: Backend contracts
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
          cache: pip
          cache-dependency-path: |
            api/requirements.txt
            config/mlops_v3_unified_environment/conda_v33.yml

      - name: Materialize pinned Azure ML dependencies
        run: |
          python -m pip install --upgrade pip==24.2
          python -m pip install pip-audit==2.10.1 pyyaml==6.0.2 pytest==8.3.5
          python - <<'PY'
          from pathlib import Path
          import yaml

          source = Path("config/mlops_v3_unified_environment/conda_v33.yml")
          dependencies = yaml.safe_load(source.read_text(encoding="utf-8"))["dependencies"]
          pip_dependencies = next(item["pip"] for item in dependencies if isinstance(item, dict) and "pip" in item)
          Path(".ci-runtime-requirements.txt").write_text(
              "\n".join(pip_dependencies) + "\n",
              encoding="utf-8",
          )
          PY
          python -m pip install -r .ci-runtime-requirements.txt -r api/requirements.txt
          python -m pip check

      - name: Audit Python dependencies
        run: |
          # Reviewed runtime exceptions are documented in docs/RUNTIME_V33_SECURITY_REVIEW_2026-09-02.md.
          python -m pip_audit -r .ci-runtime-requirements.txt --progress-spinner off \
            --ignore-vuln PYSEC-2026-3447 \
            --ignore-vuln PYSEC-2024-110 \
            --ignore-vuln CVE-2026-71211 \
            --ignore-vuln PYSEC-2026-3552 \
            --ignore-vuln PYSEC-2026-3740
          python -m pip_audit -r api/requirements.txt --progress-spinner off

      - name: Run backend tests
        env:
          PYTHONPATH: .
        run: >-
          python -m pytest -q
          -m "not integration and not requires_azure_ml and not slow"

  react:
    name: React checks
    runs-on: ubuntu-latest
    timeout-minutes: 20
    defaults:
      run:
        working-directory: react-ui
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: react-ui/package-lock.json

      - run: npm ci
      - run: npm run lint
      - run: npm test
      - run: npm run build
      - run: npx playwright install --with-deps chromium
      - run: npm run test:e2e
