# Pinned: streamlit>=1.50 broke streamlit-echarts 0.6 (uses removed components.v2 API).
# Stay on 1.49.x with echarts 0.4.0 until echarts ships a v2-compatible release.
streamlit>=1.39,<1.50
streamlit-echarts==0.4.0
requests>=2.31,<3
python-dotenv>=1.0,<2
# Charting / data — used by ui/components/{drift_gauge,drift_heatmap,metrics_charts}.py
# and several pages (Focus, Drift Monitor, Configs).
pandas>=2.0,<2.2
plotly>=5.0,<6
PyYAML>=6.0,<7
