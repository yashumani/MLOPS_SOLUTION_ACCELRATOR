[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -ra
    --strict-markers
    --tb=short
markers =
    unit: fast unit test (no Azure, no network)
    integration: requires external services (skipped in CI by default)
    requires_azure_ml: requires live Azure ML submission or downloaded Azure ML job artifacts; not local acceptance
    slow: long-running test
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
