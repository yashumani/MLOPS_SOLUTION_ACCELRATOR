name: v3_phaseb_flaml_recipe
version: 6
display_name: V3 Phase B - FLAML Recipe
type: command
inputs:
  config_name:
    type: string
  dataset_in:
    type: uri_file
  recipe_name:
    type: string
outputs:
  metrics_json:
    type: uri_file
  manifest_json:
    type: uri_file
  best_model:
    type: uri_folder
code: ../
command: >-
  python src/steps/phaseb_flaml_recipe.py
  --config configs/${{inputs.config_name}}
  --dataset_in ${{inputs.dataset_in}}
  --recipe_name ${{inputs.recipe_name}}
  --metrics_out ${{outputs.metrics_json}}
  --manifest_out ${{outputs.manifest_json}}
  --model_out ${{outputs.best_model}}
environment: azureml:mlops-v3-unified:4
